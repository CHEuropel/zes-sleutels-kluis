
import React from "react";
import ReactDOM from "react-dom/client";
import TeamVault from "./TeamVault";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <TeamVault team="Team 1" />
  </React.StrictMode>
);
